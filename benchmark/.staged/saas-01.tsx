"use client";
import { motion } from "motion/react";
import { spring } from "@/lib/motion-tokens";

// Generated by visionary-claude for saas-01 prompt.
// Style: swiss-rationalism (transplanted onto observability dashboard).
// Renders a serverless usage dashboard with disciplined Swiss grid.

const regions = [
  { name: "us-east-1",      rps: 4820, p50: 42, p95: 118, p99: 246, errors: 0.004 },
  { name: "eu-west-1",      rps: 2940, p50: 48, p95: 134, p99: 278, errors: 0.002 },
  { name: "ap-northeast-1", rps: 1670, p50: 39, p95: 104, p99: 201, errors: 0.008 },
  { name: "sa-east-1",      rps:  840, p50: 56, p95: 142, p99: 298, errors: 0.012 },
];

export default function UsageDashboard() {
  return (
    <main
      className="font-swiss grid grid-cols-12 gap-6 p-10 [background:var(--visionary-color-bg)] [color:var(--visionary-color-text)]"
      aria-label="Serverless usage dashboard"
    >
      <header className="col-span-12 border-b border-[color-mix(in_oklch,CanvasText_15%,transparent)] pb-4 mb-2">
        <p className="text-xs uppercase tracking-[0.12em] text-[color-mix(in_oklch,CanvasText_60%,transparent)]">
          Requests &mdash; last 60 min
        </p>
        <h1 className="text-5xl font-medium tracking-[-0.02em] mt-1">
          10,270 <span className="text-[var(--visionary-color-accent)]">rps</span>
        </h1>
      </header>

      {regions.map((r, i) => (
        <motion.article
          key={r.name}
          initial={{ opacity: 0, y: 8 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ ...spring.ui, delay: i * 0.04 }}
          className="col-span-6 md:col-span-3 p-5 border border-[color-mix(in_oklch,CanvasText_12%,transparent)] min-h-[44px]"
        >
          <p className="font-mono text-xs uppercase tracking-[0.08em] text-[var(--visionary-color-accent)]">{r.name}</p>
          <p className="text-3xl font-medium mt-2 tabular-nums" aria-label={`${r.rps} requests per second`}>
            {r.rps.toLocaleString()}
          </p>
          <dl className="mt-4 grid grid-cols-3 gap-2 text-xs">
            <div><dt className="text-[color-mix(in_oklch,CanvasText_55%,transparent)]">p50</dt><dd className="tabular-nums">{r.p50}ms</dd></div>
            <div><dt className="text-[color-mix(in_oklch,CanvasText_55%,transparent)]">p95</dt><dd className="tabular-nums">{r.p95}ms</dd></div>
            <div><dt className="text-[color-mix(in_oklch,CanvasText_55%,transparent)]">p99</dt><dd className="tabular-nums">{r.p99}ms</dd></div>
          </dl>
          <p className="mt-3 text-xs tabular-nums">
            err <span className={r.errors > 0.01 ? "text-[var(--visionary-color-accent)]" : ""}>{(r.errors * 100).toFixed(2)}%</span>
          </p>
        </motion.article>
      ))}

      <style>{`
        :focus-visible { outline: 3px solid AccentColor; outline-offset: 3px; }
        @media (prefers-reduced-motion: reduce) { * { animation: none !important; transition: none !important; } }
      `}</style>
    </main>
  );
}
